create table rec_events
(
 rec_event_id number primary key ,
 rec_event_reference varchar2(30),
 rec_event_src  varchar2(30)
);

create table rec_events_test
(
 rec_event_test_id number  primary key,
 rec_event_reference varchar2(30),
 rec_event_src  varchar2(30)
);

